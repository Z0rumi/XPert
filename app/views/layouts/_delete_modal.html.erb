<div data-controller="delete-modal">
    <button data-action="click->delete-modal#showModal" class="<%= delete_button_classes %>">
        <%= delete_button_text %>
    </button>
    
    <div data-delete-modal-target="modal" class="hidden modal fixed z-10 p-8 left-1/2 top-1/2 transform -translate-x-1/2 -translate-y-1/2 overflow-auto bg-white rounded shadow-lg mx-auto md:w-1/4 w-full flex flex-col relative">
        <h2 class="text-xl font-bold mb-4 text-center">
            <%= modal_title %>
        </h2>
        <div class="flex justify-between w-full">
            <%= link_to "Bestätigen", delete_path, method: :delete, data: { turbo_method: :delete }, class: "mb-4 bg-custom-red hover:bg-custom-hover-red text-white p-2 text-center border-0 rounded-md cursor-pointer transition duration-300 ease-in-out" %>
            <button data-action="click->delete-modal#hideModal" class="mb-4 bg-slate-700 hover:bg-slate-800 text-white p-2 text-center border-0 rounded-md cursor-pointer transition duration-300 ease-in-out">
                Abbrechen
            </button>
        </div>
    </div>
</div>