<div class="w-full">
  <h1 class="text-center font-bold text-4xl mb-12">Projektbearbeitung</h1>

  <%= render "form", project: @project, button_text: "Projekt speichern" %> 

  <div class="flex justify-center mt-6 space-x-4">
    <%= link_to "Zurück zu den Projekten", projects_path, class: "back-button actions flex justify-center mt-2 cursor-pointer" %>
  </div>
</div>
