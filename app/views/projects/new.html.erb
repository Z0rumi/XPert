<div class="w-full">
  <h1 class="text-center font-bold text-4xl mb-4">Projekterstellung</h1>

  <div class="mt-4">
    <%= render "form", project: @project, button_text: "Projekt erstellen" %>
  </div>

  <div class="text-center mt-5">
    <%= link_to 'Zurück zu Projekten', projects_path, class: "back-button actions flex justify-center mt-2 cursor-pointer" %>
  </div>
</div>
